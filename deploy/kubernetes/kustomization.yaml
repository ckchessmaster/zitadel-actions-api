apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  # Uncomment after creating secret.yaml from secret.example.yaml:
  # - secret.yaml
